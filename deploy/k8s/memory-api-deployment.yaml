apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronogram-memory-api
  namespace: chronogram
  labels:
    app: chronogram-memory-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chronogram-memory-api
  template:
    metadata:
      labels:
        app: chronogram-memory-api
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: memory-api
          image: ghcr.io/chronogram/chronogram-memory-api:latest
          ports:
            - name: http
              containerPort: 4000
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: chronogram-config
          env:
            - name: NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: chronogram-secrets
                  key: neo4j-password
            - name: CHRONOGRAM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: chronogram-secrets
                  key: api-key
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 15
            periodSeconds: 15
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: chronogram-memory-api
  namespace: chronogram
  labels:
    app: chronogram-memory-api
spec:
  selector:
    app: chronogram-memory-api
  ports:
    - name: http
      port: 4000
      targetPort: 4000
